<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/AutoPickupListener (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/AutoPickupListener (snapshot).java" />
              <option name="originalContent" value="package it.onlynelchilling.ultrasell.listeners;&#10;&#10;import it.onlynelchilling.ultrasell.UltraSell;&#10;import it.onlynelchilling.ultrasell.cache.CachedPlayer;&#10;import it.onlynelchilling.ultrasell.config.ConfigManager;&#10;import it.onlynelchilling.ultrasell.config.ConfigManager.SoundEntry;&#10;import it.onlynelchilling.ultrasell.utils.MessageUtils;&#10;import org.bukkit.GameMode;&#10;import org.bukkit.entity.Player;&#10;import org.bukkit.event.EventHandler;&#10;import org.bukkit.event.EventPriority;&#10;import org.bukkit.event.Listener;&#10;import org.bukkit.event.block.BlockBreakEvent;&#10;import org.bukkit.inventory.ItemStack;&#10;&#10;import java.util.Collection;&#10;import java.util.Map;&#10;&#10;public final class AutoPickupListener implements Listener {&#10;&#10;    public static final String PERMISSION = &quot;ultrasell.pickup&quot;;&#10;&#10;    private final UltraSell plugin;&#10;&#10;    public AutoPickupListener(UltraSell plugin) {&#10;        this.plugin = plugin;&#10;    }&#10;&#10;    @EventHandler(priority = EventPriority.MONITOR, ignoreCancelled = true)&#10;    public void onBlockBreak(BlockBreakEvent event) {&#10;        if (!plugin.getConfigManager().isAutoPickupEnabled()) return;&#10;        if (!event.isDropItems()) return;&#10;&#10;        Player player = event.getPlayer();&#10;        if (player.getGameMode() == GameMode.CREATIVE) return;&#10;        if (!player.hasPermission(PERMISSION)) return;&#10;&#10;        CachedPlayer cp = plugin.getPlayerCache().get(player);&#10;        if (cp == null || !cp.stats().autoPickup()) return;&#10;&#10;        Collection&lt;ItemStack&gt; drops;&#10;        try {&#10;            drops = event.getBlock().getDrops(player.getInventory().getItemInMainHand(), player);&#10;        } catch (Throwable t) {&#10;            return;&#10;        }&#10;        if (drops == null || drops.isEmpty()) return;&#10;&#10;        int totalCount = 0;&#10;        try {&#10;            for (ItemStack drop : drops) {&#10;                if (drop == null || drop.getType().isAir() || drop.getAmount() &lt;= 0) continue;&#10;                int original = drop.getAmount();&#10;                Map&lt;Integer, ItemStack&gt; leftover = player.getInventory().addItem(drop.clone());&#10;                int leftoverSum = 0;&#10;                for (ItemStack left : leftover.values()) {&#10;                    if (left != null) leftoverSum += left.getAmount();&#10;                }&#10;                totalCount += original - leftoverSum;&#10;                for (ItemStack left : leftover.values()) {&#10;                    if (left != null &amp;&amp; !left.getType().isAir() &amp;&amp; left.getAmount() &gt; 0) {&#10;                        player.getWorld().dropItemNaturally(event.getBlock().getLocation(), left);&#10;                    }&#10;                }&#10;            }&#10;        } catch (Throwable t) {&#10;            return;&#10;        }&#10;&#10;        if (totalCount &lt;= 0) return;&#10;&#10;        event.setDropItems(false);&#10;&#10;        ConfigManager cfg = plugin.getConfigManager();&#10;        if (!cfg.isAutoPickupNotify()) return;&#10;&#10;        MessageUtils msg = plugin.getMessageUtils();&#10;        msg.sendActionBar(player, &quot;auto-pickup-collected&quot;, &quot;{amount}&quot;, totalCount);&#10;&#10;        SoundEntry sound = cfg.getSellSuccessSound();&#10;        if (sound != null) player.playSound(player.getLocation(), sound.sound(), sound.volume(), sound.pitch());&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="package it.onlynelchilling.ultrasell.listeners;&#10;&#10;import it.onlynelchilling.ultrasell.UltraSell;&#10;import it.onlynelchilling.ultrasell.cache.CachedPlayer;&#10;import it.onlynelchilling.ultrasell.config.ConfigManager;&#10;import it.onlynelchilling.ultrasell.config.ConfigManager.SoundEntry;&#10;import it.onlynelchilling.ultrasell.utils.MessageUtils;&#10;import org.bukkit.GameMode;&#10;import org.bukkit.entity.Player;&#10;import org.bukkit.event.EventHandler;&#10;import org.bukkit.event.EventPriority;&#10;import org.bukkit.event.Listener;&#10;import org.bukkit.event.block.BlockBreakEvent;&#10;import org.bukkit.inventory.ItemStack;&#10;&#10;import java.util.Collection;&#10;import java.util.Map;&#10;&#10;public final class AutoPickupListener implements Listener {&#10;&#10;    public static final String PERMISSION = &quot;ultrasell.pickup&quot;;&#10;&#10;    private final UltraSell plugin;&#10;&#10;    public AutoPickupListener(UltraSell plugin) {&#10;        this.plugin = plugin;&#10;    }&#10;&#10;    @EventHandler(priority = EventPriority.MONITOR, ignoreCancelled = true)&#10;    public void onBlockBreak(BlockBreakEvent event) {&#10;        if (!plugin.getConfigManager().isAutoPickupEnabled()) return;&#10;        if (!event.isDropItems()) return;&#10;&#10;        Player player = event.getPlayer();&#10;        if (player.getGameMode() == GameMode.CREATIVE) return;&#10;        if (!player.hasPermission(PERMISSION)) return;&#10;&#10;        CachedPlayer cp = plugin.getPlayerCache().get(player);&#10;        if (cp == null || !cp.stats().autoPickup()) return;&#10;&#10;        Collection&lt;ItemStack&gt; drops;&#10;        try {&#10;            drops = event.getBlock().getDrops(player.getInventory().getItemInMainHand(), player);&#10;        } catch (Throwable t) {&#10;            return;&#10;        }&#10;        if (drops == null || drops.isEmpty()) return;&#10;&#10;        int totalCount = 0;&#10;        try {&#10;            for (ItemStack drop : drops) {&#10;                if (drop == null || drop.getType().isAir() || drop.getAmount() &lt;= 0) continue;&#10;                int original = drop.getAmount();&#10;                Map&lt;Integer, ItemStack&gt; leftover = player.getInventory().addItem(drop.clone());&#10;                int leftoverSum = 0;&#10;                for (ItemStack left : leftover.values()) {&#10;                    if (left != null) leftoverSum += left.getAmount();&#10;                }&#10;                totalCount += original - leftoverSum;&#10;                for (ItemStack left : leftover.values()) {&#10;                    if (left != null &amp;&amp; !left.getType().isAir() &amp;&amp; left.getAmount() &gt; 0) {&#10;                        player.getWorld().dropItemNaturally(event.getBlock().getLocation(), left);&#10;                    }&#10;                }&#10;            }&#10;        } catch (Throwable t) {&#10;            return;&#10;        }&#10;&#10;        if (totalCount &lt;= 0) return;&#10;&#10;        event.setDropItems(false);&#10;        plugin.getLogger().fine(() -&gt; &quot;[AutoPickup] suppressed vanilla drops for &quot; + player.getName() + &quot; (collected=&quot; + totalCount + &quot;)&quot;);&#10;&#10;        ConfigManager cfg = plugin.getConfigManager();&#10;        if (!cfg.isAutoPickupNotify()) return;&#10;&#10;        MessageUtils msg = plugin.getMessageUtils();&#10;        msg.sendActionBar(player, &quot;auto-pickup-collected&quot;, &quot;{amount}&quot;, totalCount);&#10;&#10;        SoundEntry sound = cfg.getSellSuccessSound();&#10;        if (sound != null) player.playSound(player.getLocation(), sound.sound(), sound.volume(), sound.pitch());&#10;    }&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="/SchedulerUtil (snapshot).java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/SchedulerUtil (snapshot).java" />
              <option name="originalContent" value="package it.onlynelchilling.ultrasell.utils;&#10;&#10;import org.bukkit.Bukkit;&#10;import org.bukkit.entity.Entity;&#10;import org.bukkit.plugin.Plugin;&#10;&#10;import java.lang.invoke.MethodHandle;&#10;import java.lang.invoke.MethodHandles;&#10;import java.lang.invoke.MethodType;&#10;import java.util.function.Consumer;&#10;&#10;public final class SchedulerUtil {&#10;&#10;    private static final boolean FOLIA;&#10;    private static MethodHandle GET_ASYNC_SCHEDULER;&#10;    private static MethodHandle ASYNC_RUN_NOW;&#10;    private static MethodHandle GET_GLOBAL_SCHEDULER;&#10;    private static MethodHandle GLOBAL_RUN;&#10;    private static MethodHandle ENTITY_GET_SCHEDULER;&#10;    private static MethodHandle ENTITY_RUN;&#10;    private static MethodHandle ENTITY_RUN_DELAYED;&#10;&#10;    static {&#10;        boolean folia;&#10;        try {&#10;            Class.forName(&quot;io.papermc.paper.threadedregions.RegionizedServer&quot;);&#10;            folia = true;&#10;        } catch (ClassNotFoundException e) {&#10;            folia = false;&#10;        }&#10;        FOLIA = folia;&#10;        if (FOLIA) {&#10;            try {&#10;                MethodHandles.Lookup lookup = MethodHandles.lookup();&#10;                Class&lt;?&gt; asyncScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.AsyncScheduler&quot;);&#10;                Class&lt;?&gt; globalScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.GlobalRegionScheduler&quot;);&#10;                Class&lt;?&gt; entityScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.EntityScheduler&quot;);&#10;                Class&lt;?&gt; scheduledTask = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.ScheduledTask&quot;);&#10;&#10;                GET_ASYNC_SCHEDULER = lookup.findVirtual(Bukkit.getServer().getClass(), &quot;getAsyncScheduler&quot;, MethodType.methodType(asyncScheduler));&#10;                ASYNC_RUN_NOW = lookup.findVirtual(asyncScheduler, &quot;runNow&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class));&#10;                GET_GLOBAL_SCHEDULER = lookup.findVirtual(Bukkit.getServer().getClass(), &quot;getGlobalRegionScheduler&quot;, MethodType.methodType(globalScheduler));&#10;                GLOBAL_RUN = lookup.findVirtual(globalScheduler, &quot;run&quot;, MethodType.methodType(void.class, Plugin.class, Consumer.class));&#10;                ENTITY_GET_SCHEDULER = lookup.findVirtual(Entity.class, &quot;getScheduler&quot;, MethodType.methodType(entityScheduler));&#10;                ENTITY_RUN = lookup.findVirtual(entityScheduler, &quot;run&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class, Runnable.class));&#10;                ENTITY_RUN_DELAYED = lookup.findVirtual(entityScheduler, &quot;runDelayed&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class, Runnable.class, long.class));&#10;            } catch (Exception e) {&#10;                throw new RuntimeException(&quot;Failed to init Folia scheduler handles&quot;, e);&#10;            }&#10;        }&#10;    }&#10;&#10;    public static boolean isFolia() {&#10;        return FOLIA;&#10;    }&#10;&#10;    public static void runAsync(Plugin plugin, Runnable task) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = GET_ASYNC_SCHEDULER.invoke(Bukkit.getServer());&#10;                ASYNC_RUN_NOW.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run());&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTaskAsynchronously(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runSync(Plugin plugin, Runnable task) {&#10;        if (FOLIA) {&#10;            Bukkit.getGlobalRegionScheduler().run(plugin, t -&gt; task.run());&#10;        } else {&#10;            Bukkit.getScheduler().runTask(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runForEntity(Plugin plugin, Entity entity, Runnable task, Runnable retired) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = ENTITY_GET_SCHEDULER.invoke(entity);&#10;                ENTITY_RUN.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run(), retired);&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTask(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runForEntityLater(Plugin plugin, Entity entity, Runnable task, Runnable retired, long delayTicks) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = ENTITY_GET_SCHEDULER.invoke(entity);&#10;                ENTITY_RUN_DELAYED.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run(), retired, delayTicks);&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTaskLater(plugin, task, delayTicks);&#10;        }&#10;    }&#10;}&#10;" />
              <option name="updatedContent" value="package it.onlynelchilling.ultrasell.utils;&#10;&#10;import org.bukkit.Bukkit;&#10;import org.bukkit.entity.Entity;&#10;import org.bukkit.plugin.Plugin;&#10;&#10;import java.lang.invoke.MethodHandle;&#10;import java.lang.invoke.MethodHandles;&#10;import java.lang.invoke.MethodType;&#10;import java.util.function.Consumer;&#10;&#10;public final class SchedulerUtil {&#10;&#10;    private static final boolean FOLIA;&#10;    private static MethodHandle GET_ASYNC_SCHEDULER;&#10;    private static MethodHandle ASYNC_RUN_NOW;&#10;    private static MethodHandle GET_GLOBAL_SCHEDULER;&#10;    private static MethodHandle GLOBAL_RUN;&#10;    private static MethodHandle ENTITY_GET_SCHEDULER;&#10;    private static MethodHandle ENTITY_RUN;&#10;    private static MethodHandle ENTITY_RUN_DELAYED;&#10;&#10;    static {&#10;        boolean folia;&#10;        try {&#10;            Class.forName(&quot;io.papermc.paper.threadedregions.RegionizedServer&quot;);&#10;            folia = true;&#10;        } catch (ClassNotFoundException e) {&#10;            folia = false;&#10;        }&#10;        FOLIA = folia;&#10;        if (FOLIA) {&#10;            try {&#10;                MethodHandles.Lookup lookup = MethodHandles.lookup();&#10;                Class&lt;?&gt; asyncScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.AsyncScheduler&quot;);&#10;                Class&lt;?&gt; globalScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.GlobalRegionScheduler&quot;);&#10;                Class&lt;?&gt; entityScheduler = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.EntityScheduler&quot;);&#10;                Class&lt;?&gt; scheduledTask = Class.forName(&quot;io.papermc.paper.threadedregions.scheduler.ScheduledTask&quot;);&#10;&#10;                GET_ASYNC_SCHEDULER = lookup.findVirtual(Bukkit.getServer().getClass(), &quot;getAsyncScheduler&quot;, MethodType.methodType(asyncScheduler));&#10;                ASYNC_RUN_NOW = lookup.findVirtual(asyncScheduler, &quot;runNow&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class));&#10;                GET_GLOBAL_SCHEDULER = lookup.findVirtual(Bukkit.getServer().getClass(), &quot;getGlobalRegionScheduler&quot;, MethodType.methodType(globalScheduler));&#10;                GLOBAL_RUN = lookup.findVirtual(globalScheduler, &quot;run&quot;, MethodType.methodType(void.class, Plugin.class, Consumer.class));&#10;                ENTITY_GET_SCHEDULER = lookup.findVirtual(Entity.class, &quot;getScheduler&quot;, MethodType.methodType(entityScheduler));&#10;                ENTITY_RUN = lookup.findVirtual(entityScheduler, &quot;run&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class, Runnable.class));&#10;                ENTITY_RUN_DELAYED = lookup.findVirtual(entityScheduler, &quot;runDelayed&quot;, MethodType.methodType(scheduledTask, Plugin.class, Consumer.class, Runnable.class, long.class));&#10;            } catch (Exception e) {&#10;                throw new RuntimeException(&quot;Failed to init Folia scheduler handles&quot;, e);&#10;            }&#10;        }&#10;    }&#10;&#10;    public static boolean isFolia() {&#10;        return FOLIA;&#10;    }&#10;&#10;    public static void runAsync(Plugin plugin, Runnable task) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = GET_ASYNC_SCHEDULER.invoke(Bukkit.getServer());&#10;                ASYNC_RUN_NOW.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run());&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTaskAsynchronously(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runSync(Plugin plugin, Runnable task) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = GET_GLOBAL_SCHEDULER.invoke(Bukkit.getServer());&#10;                GLOBAL_RUN.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run());&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTask(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runForEntity(Plugin plugin, Entity entity, Runnable task, Runnable retired) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = ENTITY_GET_SCHEDULER.invoke(entity);&#10;                ENTITY_RUN.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run(), retired);&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTask(plugin, task);&#10;        }&#10;    }&#10;&#10;    public static void runForEntityLater(Plugin plugin, Entity entity, Runnable task, Runnable retired, long delayTicks) {&#10;        if (FOLIA) {&#10;            try {&#10;                Object scheduler = ENTITY_GET_SCHEDULER.invoke(entity);&#10;                ENTITY_RUN_DELAYED.invoke(scheduler, plugin, (Consumer&lt;?&gt;) t -&gt; task.run(), retired, delayTicks);&#10;            } catch (Throwable e) {&#10;                throw new RuntimeException(e);&#10;            }&#10;        } else {&#10;            Bukkit.getScheduler().runTaskLater(plugin, task, delayTicks);&#10;        }&#10;    }&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>